/** @format */

import type { Lang, PlaceType } from '../../models/models';
import { allTypes, translations } from '../../constants/type';

export { allTypes, translations };
export type { PlaceType, Lang };
