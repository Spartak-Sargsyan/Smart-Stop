/** @format */

import type { Lang } from '../../models/models';
import { languageSelector } from '../../constants/language';
import { useLanguage } from '../../context/LanguageContext';

export { languageSelector, useLanguage };
export type { Lang };
